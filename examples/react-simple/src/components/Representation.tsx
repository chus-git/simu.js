import * as THREE from 'three';
import { useState } from 'react';

function Representation() {

    const [threeScene, setThreeScene] = useState(new THREE.Scene());

    function initScene() {

        // Init scene

    }

    return (
        <>

        </>
    );

}

export default Representation;